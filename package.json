{
  "name": "thumbtack_ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@apollo/client": "^3.5.5",
    "@testing-library/jest-dom": "^5.16.0",
    "@testing-library/react": "^11.2.7",
    "@testing-library/user-event": "^12.8.3",
    "@types/jest": "^26.0.24",
    "@types/node": "^12.20.37",
    "@types/react": "^17.0.37",
    "@types/react-dom": "^17.0.11",
    "graphql": "^16.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.0.2",
    "react-scripts": "4.0.3",
    "sass": "^1.44.0",
    "typescript": "^4.5.2",
    "use-local-storage": "^2.2.5",
    "web-vitals": "^1.1.2"
  },
  "scripts": {
    "start": "export NODE_OPTIONS=--openssl-legacy-provider; react-scripts start",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider; react-scripts build",
    "test": "export NODE_OPTIONS=--openssl-legacy-provider; start-server-and-test start http-get://localhost:3000 cy:run",
    "eject": "export NODE_OPTIONS=--openssl-legacy-provider; react-scripts eject",
    "cypress": "./node_modules/.bin/cypress open"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cypress": "^9.1.1",
    "start-server-and-test": "^1.14.0"
  }
}
